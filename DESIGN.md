# 10-eok — DESIGN.md

> AI 코딩 에이전트용 디자인 언어. 이 문서를 기준으로 UI를 일관되게 생성한다.
> 기반: Spotify 디자인 시스템(다크 + 형광 그린). 출처 awesome-design-md/spotify.

## 0. 제품 한 줄
"내 적립으로 10억까지, 며칠 걸렸을까?"를 보여주는 백테스트 웹앱. 영웅은 **극적인 단일
숫자**, 분위기는 **축하·공유 가능**(Spotify Wrapped 결). 모바일 우선.

## 1. 미학 원칙
- **Content-first darkness:** 검정 캔버스 위에 UI는 물러나고, 형광 그린과 큰 숫자가
  유일한 색·주목점. 산만한 장식 금지.
- **Pill geometry:** 모든 버튼은 알약형(둥근 끝). 터치 최적.
- **숫자가 영웅:** hero 숫자는 화면에서 가장 크고 굵게. 나머지는 보조.
- **축하 모먼트:** 목표 달성 결과엔 그린으로 작은 성취감. 과하지 않게.
- **무채색 + 단일 그린 액센트:** 그린(#1ed760)은 강조에만. 색을 늘리지 않는다.

## 2. 컬러 토큰
```
--bg:        #121212   /* 페이지 배경 */
--surface:   #181818   /* 카드 */
--surface-2: #1f1f1f   /* 입력칩, 보조 버튼 */
--ink:       #ffffff   /* 본문 흰색 */
--muted:     #b3b3b3   /* 보조 텍스트 */
--line:      #2a2a2a   /* 구분선 */
--accent:    #1ed760   /* 형광 그린 — 강조/긍정/CTA */
--accent-alt:#1db954   /* 그린 변형 */
--neg:       #f3727f   /* 손실/에러 */
--warn:      #ffa42b
```

## 3. 타이포그래피
- 폰트: 영문/숫자 — system-ui 또는 Pretendard(한글 우선 권장). 굵기 400/600/700.
- 스케일 (실제 구현값 — CSS 토큰 `--fs-*`로 globals.css `:root`에서 단일 관리):
  - 결과 영웅 숫자 `--fs-hero`: 62px / 700 / letter-spacing -.03em (단위 '년·개월'은 28px muted)
  - 인트로 헤드라인 `--fs-h1`: 40px / 700
  - 본문 페이지 제목 `--fs-article-h1`: 30px / 700
  - 섹션 타이틀 `--fs-section`: 20px / 700
  - Feature: 18px / 600
  - Body `--fs-body`: 16px / 400 (Bold 700)
  - 라벨(칩 키, 통계 키): 10–12px / 700 / **uppercase 금지(한글)** — 한글은 그냥 작게.
  - 버튼: 14px / 700, **영문일 때만** uppercase + letter-spacing 1.4–2px. 한글 버튼은
    uppercase 미적용(한글엔 대문자 개념 없음) — 굵게 + 자간 살짝.
- 숫자는 `font-variant-numeric: tabular-nums` (자릿수 정렬).

## 4. 컴포넌트
- **입력칩(종목/매달/매수일):** `--surface-2` 배경, radius 8px, shadow `rgba(0,0,0,.3) 0 8px 8px`.
  키(작은 muted) + 값(15px/700 흰색). 가로 3분할.
- **Hero 블록:** 가운데 정렬. 위에 그린 소제목(작게) → 거대한 숫자 → 기간 보조선(muted).
- **성장 카드:** `--surface` 배경, radius 8px, dialog shadow `rgba(0,0,0,.5) 0 8px 24px`.
  상단 라벨 행(좌: 자산 성장 / 우: 목표 10억), 그린 라인차트 + 그린 그라데이션 면,
  목표선은 점선, 도달 지점에 흰 점. 하단 통계 3분할(상단 구분선).
- **공유 버튼:** 그린(#1ed760) 알약, 검정 텍스트(#000), 굵게. radius 500px, padding 14px.
- **라디우스 스케일:** badge 2 / input 4 / card 6–8 / panel 10–20 / pill 500 / circle 50%.
- **그림자:** elevated `rgba(0,0,0,.3) 0 8px 8px` / dialog `rgba(0,0,0,.5) 0 8px 24px`.
- **간격:** 8px 베이스 (8/12/16/20/24…).

## 5. 쉬운 말 카피 원칙 (중요 — 일반 유저 대상)
금융 전문용어는 일반 사용자가 막힌다. 그렇다고 유치하게(예: "불어난 돈") 가지도 않는다.
**일반 성인이 평소 쓰는 자연스러운 워딩**을 표면에 쓰고, 정확한 전문용어는 작은 ⓘ 툴팁으로
보충한다. 톤: 전문용어 ❌, 애기말 ❌, 일반 성인 일상어 ✅.

용어 변환 사전(표면 라벨 = 일반 워딩):
| 전문용어(쓰지 말 것) | 화면 표기(일반 워딩) | ⓘ 툴팁(정확한 설명) |
|---|---|---|
| CAGR / 연복리수익률 | **연평균** (+21%) | "1년에 평균 +21%씩 늘었다는 뜻 (연복리 수익률)" |
| 납입원금 | **원금** | "그동안 넣은 금액 합계" |
| 평가액 / 평가금액 | **최종 금액** (또는 총 금액) | "그 시점의 평가금액" |
| 정액적립 / 적립식 | **매달 넣기 / 매달 투자** | — |
| 수익률 | **얼마나 늘었나** | — |
| 백테스트 | **과거에 했다면** | "실제 과거 데이터로 돌려본 결과" |
| 매수일 | **사는 날** (또는 '매수일') | "매달 며칠에 살지" |
| 환율 반영 | **그날 환율로** | "달러 ETF라 산 날 환율로 계산" |

원칙:
- 숫자 + 일반 워딩이 먼저. 정확한 전문용어는 ⓘ 안에.
- 단위는 한글로: "13년 7개월", "1.63억", "+21%".
- 결과 문장은 자연스럽게: "13년 7개월이면 10억을 모았어요."
- 담백하게. 과장·이모지 남발 금지(그린 액센트로 충분).

## 6. 상태 (필수 구현) — 확정 카피
- **로딩:** 스피너 + "계산 중…" (또는 스켈레톤). 무한 로딩 금지.
- **에러:** "데이터를 못 불러왔어요 · 잠시 후 다시 시도해 주세요" + [다시 시도] 버튼.
- **목표 미달:** lead를 그린 대신 muted "아직 10억은 멀어요"로, hero는 경과 기간,
  최종 금액은 실제 평가액(10억 아님), 도달점은 목표선 아래. span "20년 모았는데 지금 X억 · 계속 모으는 중".
- **연평균 툴팁:** ⓘ 탭/호버 → "연평균 +21%는 1년에 평균 이만큼씩 늘었다는 뜻이에요. (QLD 과거 수익률 기준)".
- **즉시 반응:** 입력 바꾸면 100ms 안에 숫자·그래프 갱신.

### 확정 마이크로카피 (프로토타입 검증 완료)
- 인트로 키커: "만약에, 10억" / 헤드라인: "과거에 매달 모았다면, 10억까지 얼마나 걸렸을까?"
- 인트로 CTA: "내 결과 보기" / 힌트: "3가지만 정하면 끝나요 · 약 20초"
- 챗 질문: "어떤 종목으로 해볼까요?" / "매달 얼마씩 넣을까요?" / "매달 며칠에 살까요?" / 완료 "결과 보기 →"
- 미출시 종목 칩: "준비 중" (NOT "곧 추가")
- 결과 hero lead: "10억 모으기까지" / 통계: 원금 · 최종 금액 · 연평균
- 톤: 존댓말 일관. 헤드라인의 시간 표현은 "며칠"이 아니라 "얼마나"(결과가 년/개월이라).

## 7. 참고
- 시안 원본: `~/.gstack/projects/10-eok/designs/mockups/spotify.html`
- 차트는 5천 포인트라 표시용 다운샘플(LTTB) 또는 라이브러리 데시메이션 사용.
